# 루틴 프롬프트 — 증량 식단 (하루 일곱 번)

식단 페이지: https://claude.ai/code/artifact/a2c4e93d-1c33-42bd-9ebb-c9b11791f29d

루틴은 모두 **발동할 때마다 새 세션**을 열고 **푸시 알림**을 켜 두었다.
cron은 UTC 기준이므로 원하는 KST 시각에서 9시간을 뺀다. 실제 발동은 등록한 분(分)에서
몇 분쯤 밀릴 수 있다 (서버가 루틴을 시간대에 흩어 놓는다).

| 루틴 | KST | cron (UTC) | 하는 일 |
| --- | --- | --- | --- |
| 증량 식단 — 매일 아침 7시 오늘 식단 | 07:00 | `0 22 * * *` | 그날 식단을 지어 db에 넣고 하루치를 알린다 |
| 식사 알림 — 07:30 아침 | 07:30 | `30 22 * * *` | `m1` |
| 식사 알림 — 10:30 쉐이크 | 10:30 | `30 1 * * *` | `m2` |
| 식사 알림 — 12:30 점심 도시락 | 12:30 | `30 3 * * *` | `m3` |
| 식사 알림 — 16:00 간식 | 16:00 | `0 7 * * *` | `m4` |
| 식사 알림 — 19:00 저녁 | 19:00 | `0 10 * * *` | `m5` + 오늘 밤 할 일 |
| 식사 알림 — 22:00 야식 | 22:00 | `0 13 * * *` | `m6` + 내일 도시락 채근 |

날짜는 어느 루틴에서든 `date -u -d '+9 hours' +%F` 로 구한다. 22:00 UTC에 도는
아침 루틴은 이미 KST로 다음 날이므로 이 식이 그대로 맞다.

---

## 아침 7시 루틴 (식단을 짓는 쪽)

아래가 트리거에 등록된 지시문 원본이다. 고쳤으면 트리거에도 반영할 것.

### 지켜야 하는 전제 (매일 같다)

- 목적은 증량이다. 체중 65~75kg대, 근육 위주로 +5~8kg가 목표.
- **위가 약해 한 번에 많이 못 먹는다.** 그래서 하루 여섯 끼로 쪼갠다. 한 끼를 크게 만들지 말 것.
- **유제품이 안 받는다.** 우유·치즈·요거트·생크림 금지. 전부 무가당 두유·락토프리 우유·아몬드유로 대체한다.
- **점심은 도시락이다.** 12:30 끼니는 전날 밤에 싸서 들고 갈 수 있어야 한다 — 식어도 먹을 만하고, 국물이 없고, 통에 담기는 것.
- 튀김·과하게 매운 것·빈속 커피는 넣지 않는다.
- 부피가 크고 열량이 낮은 것(국물, 샐러드, 탄산)은 최소로. 기름 한 작은술씩을 나물·볶음에 얹어 밀도를 올린다.

### 주차와 목표 열량

1주차 시작일은 **2026-09-10**. 주차 = (오늘 − 2026-09-10)일수 ÷ 7 + 1.

| 주차 | kcalTarget | proteinTarget |
| --- | --- | --- |
| 1주차 | 2,850 | 150g |
| 2주차 | 3,000 | 160g |
| 3주차부터 | 3,200 | 170g |

한 번에 3,200으로 올리지 않는 것은 위 때문이다. 위가 여섯 끼에 적응하기 전에 양을 올리면
체하고, 체하면 그날이 아니라 사흘을 잃는다.

### 절차

1. 날짜와 주차를 구한다.
2. `read_db` / `query` / `siktan` / `order_by date desc, limit 7` 로 지난 이레를 읽는다.
   오늘 문서가 이미 있으면 새로 짓지 말고 알림만 보낸다. 점심·저녁 단백질은 이틀 연속 겹치지 않게 한다.
3. 여섯 끼를 짓는다. **kcal 합 = kcalTarget, protein 합 = proteinTarget** 이어야 하고, 파이썬으로 더해 확인한다.
4. `write_db` / `set` / `siktan` / `doc_id`=오늘 날짜 로 올린다. **HTML은 건드리지 않는다** — db에 넣으면 페이지가 알아서 바뀐다.
5. 여덟 줄 이내 한국어로 끝맺는다. 이것이 휴대폰 알림이 된다.

## 식사 알림 루틴 (읽어서 알리기만 하는 쪽)

여섯 개가 같은 꼴이다. `m1`~`m6`과 시각·이모지만 다르다.

```
지금은 한국시간 12:30, 점심 도시락 먹을 시간이다. 아래만 하고 끝내라.

1. `bash date -u -d '+9 hours' +%F` 로 오늘 날짜(KST)를 구한다.
2. Artifact 도구로 read_db / get / collection "siktan" / doc_id 오늘 날짜.
3. meals 배열에서 id가 `m3`인 끼니를 찾는다.
4. 문서가 없으면 "오늘 식단이 아직 안 올라왔다"고만 알리고 끝낸다.

마지막 답변은 한국어 여섯 줄 이내. 서론·잔소리 없이:
- 첫 줄: `🍱 12:30 점심 도시락 · ○○○kcal / 단백질 ○○g`
- 다음 줄들: items를 `· 이름 분량` 으로 한 줄씩
- 마지막 줄: 그 끼니의 tip 한 문장
다른 도구는 부르지 말 것.
```

19:00과 22:00만 여기에 `prep`(그날 밤 할 일)을 덧붙인다. 내일 도시락이 여기에 걸려 있다.

## db 서식

```
siktan/<YYYY-MM-DD>
{ "date":"2026-09-10", "dow":"목", "week":1,
  "kcalTarget":2850, "proteinTarget":150, "note":"오늘 하루의 한 줄",
  "meals":[ { "id":"m1", "time":"07:30", "name":"아침", "kcal":500, "protein":24,
              "items":[ ["흰쌀밥","180g",265], ... ],
              "tip":"그 끼니에서 걸려 넘어질 만한 것 한 문장" }, ... ],
  "prep":["오늘 밤에 해둘 일"], "shopping":["장 볼 것"] }

log/<YYYY-MM-DD>      { "date":"...", "done":{"m1":true,...} }   페이지가 쓴다
weight/<YYYY-MM-DD>   { "date":"...", "kg":70.4 }                페이지가 쓴다
```

`items`는 `[이름, 분량, kcal]` 세 칸이다. 분량은 g·ml·개수로 재서 알 수 있게 — "적당히" 금지.

## 고치는 법

- **시각을 바꾸려면** — 해당 루틴의 cron을 고친다 (KST − 9시간).
- **열량·주차를 바꾸려면** — 아침 7시 루틴 프롬프트의 주차 표를 고친다.
- **끼니 수나 시각 자체를 바꾸려면** — 아침 루틴의 `m1`~`m6` 목록, 해당 식사 알림 루틴의 cron,
  그리고 `siktan.html`의 SEED를 함께 고쳐야 한다.
- **디자인을 바꾸려면** — `siktan.html`을 고쳐 같은 URL로 다시 배포한다.
- **알림이 성가시면** — claude.ai의 Routines에서 개별 루틴을 끄면 된다. 나머지는 그대로 돈다.
